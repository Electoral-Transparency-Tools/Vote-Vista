# dependencies
/node_modules
/.pnp
.pnp.js

# next.js / build output
/.next
/out
/build
next-env.d.ts
*.tsbuildinfo

# env files (never commit secrets / API keys)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# deploy
/.vercel

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# misc / OS
.DS_Store
*.pem
coverage

# Gathered data now lives in the Neon database (the source of truth).
# These files are kept locally only, as inputs for `npm run db:seed`.
/data/*
!/data/location_meta.json
# Public constituency boundaries (LGD/CC) — tracked as a reproducible seed input.
!/data/bangalore_constituencies.geojson
/sources/
